/* TODO
import { Controller } from './BaseController.js';

const currentUser = new Controller('');

currentUser.read = async (req, res) => {
	if (currentUser.id !== null) {
		res.render('pages/tt2_mainpage_skeleton.ejs', { userstatus_name: currentUser.username, userstatus_page: currentUser.username });
	} else {
		res.render('pages/tt2_mainpage_skeleton.ejs', { userstatus_name: 'Login/Sign Up', userstatus_page: 'login' });
	}
};
*/
